# OpenCalcs Web

The application UI will live here as a separate frontend and consume the OpenCalcs API.

Planned first implementation: Next.js + React + TypeScript, with generic schema-driven calculation forms and custom components for map-heavy workflows such as OpenWind.
